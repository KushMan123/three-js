/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 EarthAtmosphere.glb 
*/

import React from 'react'
import { useGLTF, useTexture } from '@react-three/drei'
import * as THREE from 'three'

export function EarthAtmosphere(props) {
  const { nodes, materials } = useGLTF('./models/spaceship/EarthAtmosphere.glb')
  
  // Load atmosphere texture with alpha/transparency
  const atmosphereTexture = useTexture('textures/EarthAtmosphere.png')
  atmosphereTexture.flipY = false
  atmosphereTexture.encoding = THREE.sRGBEncoding
  
  // Create transparent material for atmosphere
  const atmosphereMaterial = new THREE.MeshStandardMaterial({
    map: atmosphereTexture,
    transparent: true,
    opacity: 0.8, // Adjust opacity as needed (0 = fully transparent, 1 = opaque)
    alphaMap: atmosphereTexture, // Use the texture's alpha channel if it has one
    side: THREE.DoubleSide, // Render both sides for atmosphere
  })
  
  return (
    <group {...props} dispose={null}>
      <mesh geometry={nodes.EarthAtmosphere.geometry} material={atmosphereMaterial} />
    </group>
  )
}

useGLTF.preload('./models/spaceship/EarthAtmosphere.glb')
